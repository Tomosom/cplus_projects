/*
 * 基本思想：
 *  根据邻接矩阵法中的残留问题，
 *  为了进一步提高空间使用率，可以考虑使用链表替换数组，将邻接矩阵变换为邻接链表。。
 * 
 * 邻接链表法：
 *  - 图中的所有顶点按照编号存储于同一个链表中
 *  - 每一个顶点对应一个链表，用于存储始发与该顶点的边
 *  - 每一条边的信息包含：起点，终点，权值
 */
#ifndef LINK_GRAPH
#define LINK_GRAPH

#include "graph.h"


namespace DTLib {




}



#endif